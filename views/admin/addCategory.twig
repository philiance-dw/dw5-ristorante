{% extends "layout/admin.twig" %}

{% import "macros/forms.twig" as form %}

{% block main %}
	<form class="add-item-form" method="POST">
		{{form.input('name', data.name, 'Nom', 'categoryName', 'Le nom de la catégorie', errors.name, 'text', true)}}
		<button class="btn">{{editMode ? 'Modifier': 'Ajouter'}}</button>
	</form>
{% endblock %}
